<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dueeeke.customviewcollect">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".guide.TestGuideAcitivity"></activity>
        <activity android:name=".like.TestLikeActivity" />
        <activity android:name=".dragmsg.TestBubbleActivity" />
        <activity android:name=".loading.TestLoadingActivity" />
        <activity
            android:name=".behavior.TestBehaviorActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".alphabetindex.TestAlphabetActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".qqstep.TestStepActivity" />
        <activity android:name=".trackcolor.TestTrackColorActivity" />
    </application>

</manifest>